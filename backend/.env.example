# MongoDB Connection (Required)
# For MongoDB Atlas, get connection string from: https://cloud.mongodb.com
# Format: mongodb+srv://username:password@cluster.mongodb.net/
MONGO_URL=mongodb+srv://your_username:your_password@cluster.mongodb.net/
DB_NAME=dsp_photography

# Frontend URL (Required for CORS)
# Set to your deployed frontend URL (Vercel, Netlify, etc.)
# For local development: http://localhost:3000
# For production: https://your-domain.vercel.app
FRONTEND_URL=https://your-frontend-domain.vercel.app

# AI Integration (Optional - for content generation features)
# Groq API: https://console.groq.com
GROQ_API_KEY=your_groq_api_key_here

# Google Gemini API: https://ai.google.dev
GEMINI_API_KEY=your_gemini_api_key_here

# Security Keys (Required)
# Generate strong random strings for these
# Emergency reset key for admin password recovery
EMERGENCY_RESET_KEY=your_strong_random_secret_key

# JWT secret for token signing
JWT_SECRET=your_jwt_secret_key_here

# Server Configuration
# Port the backend server will run on
PORT=8000
HOST=0.0.0.0

# Upload Directory (Optional)
# Directory for file uploads (defaults to ./uploads if not set)
UPLOAD_DIR=uploads